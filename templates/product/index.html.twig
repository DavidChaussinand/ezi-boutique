{% extends 'base.html.twig' %}

{% block title %}Nos produits{% endblock %}


{% block navbar %}
     {{ include ('layout/othernavbar.html.twig')}}
{% endblock %}

{% block body %}
<h1> Nos produits</h1>


<div class="row">
    
    {% for product in products %}
        <div class="col-md-4">
            <div class="product-item text-center">
               <a href="{{ path ('app_product', { 'slug' : product.slug}) }} "><img src="/uploads/files/{{product.illustration}}" alt="{{ product.name}} " class="img-fluid"></a> 
                <h5>{{ product.name}}</h5>
                <span> {{product.surnom}}</span>
                <span> {{(product.price / 100)| number_format(2,',')  }} €</span>
                 
            </div>
        </div>
    {% endfor %}


</div>


{% endblock %}
