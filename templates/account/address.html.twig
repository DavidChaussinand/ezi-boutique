{% extends 'base.html.twig' %}

{% block title %}Mes adresses : Ezi{% endblock %}


{% block navbar %}
     {{ include ('layout/othernavbar.html.twig')}}
{% endblock %}

{% block body %}


    <a href=" {{ path('app_account_address_add') }}" class="btn btn-info btn-small float-end">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    C'est dans cette espace que vous allez pouvoir gérer vos adresses.

    <a href=" {{ path('app_account')}}">Retour</a>

        <hr>
        <br>
  
        {% if app.user.addresses|length == 0 %}
         Vous n'avez pas encore d'adresse dans votre compte client. Pour en ajouter une , veuillez  <a href="{{ path('app_account_address_add') }}">cliquer ici</a> 

         {% else %}
         
         
         <div class="row">

                {% for address in app.user.addresses %}

                <div class="col-md-4">
                    <div class="card" style="width: 18rem;">
                    
                        <div class="card-body">
                        <h5 class="card-title">{{ address.name }}</h5>
                        <p class="card-text">
                             {{ address.address}} , <br> 
                           {{ address.postal}} - {{ address.city}}
                        
                        </p>
                        <a href="{{ path('app_account_address_edit', {'id' : address.id } ) }}" class="btn text-primary">Modifier</a> |  <a href="{{ path('app_account_address_delete', {'id' : address.id } ) }}" class="btn text-primary">Supprimer</a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>

        {% endif %}
        

  
{% endblock %}