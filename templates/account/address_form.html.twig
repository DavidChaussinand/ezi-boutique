{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse : Ezi{% endblock %}


{% block navbar %}
     {{ include ('layout/othernavbar.html.twig')}}
{% endblock %}

{% block body %}


    <h1>Gérer mes adresses</h1>

   

    <a href=" {{ path('app_account_address')}}">Retour</a>

        <hr>
        <br>
  

         {{ form_start(form) }}

            <div class="row">
                <div class="col-md-12 mb-2">{{ form_row(form.name)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.firstname)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.lastname)}}</div>
                <div class="col-md-12 mb-2">{{ form_row(form.company)}}</div>
               
                <div class="col-md-6 mb-2">{{ form_row(form.address)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.postal)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.city)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.country)}}</div>
                <div class="col-md-6 mb-2">{{ form_row(form.phone)}}</div>
             
              
            </div>

         {{ form_end(form) }}


  
        

   
{% endblock %}