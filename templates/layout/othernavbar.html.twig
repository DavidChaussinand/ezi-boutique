<header class="header-background-black text-bg-dark p-3">
    <nav class="navbar navbar-expand-lg bg-body-black ">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home')}}">Ezi</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 fs-4">
                    <li class="nav-item  ">
                        <a class="nav-link active  text-light " aria-current="page" href="{{ path('app_products')}}">Nos produits</a>
                    </li>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle  text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Qui sommes nous
                        </a>
                        <ul class="dropdown-menu fs-5">
                            <li><a class="dropdown-item" href="{{ path('app_point_of_sale')}}">Nos lieux de ventes</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_contact') }}">Nous contacter</a></li>
                        </ul>
                    </li>
                    
                </ul>
                <ul class="navbar-nav fs-4">
                    {% if app.user %}
                        <li class="nav-item  text-light">
                            <a class="nav-link  text-light" href="{{ path('app_account') }}">Mon compte</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link  text-light" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link  text-light" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link  text-light" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
            <a href="{{ path('app_cart')}}">
                <img src="/image/shopping-cart.png" class="cart-icon" alt="mon panier">

            </a>
        </div>
    </nav>
</header>
